# @package _global_

defaults:
  - pointnav_base_depth
  - /habitat/dataset/pointnav: gibson
  - _self_

habitat:
  environment:
    max_episode_steps: 500
  simulator:
    agents:
      main_agent:
        sim_sensors:
          depth_sensor:
            width: 256
            height: 256
